{
  "name": "mc-shutdown",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "deploy": "zip -r mc-shutdown.zip . && aws lambda update-function-code --function-name mc-shutdown --zip-file fileb://mc-shutdown.zip && rm mc-shutdown.zip"
  },
  "dependencies": {
    "axios": "^0.19.0"
  }
}
